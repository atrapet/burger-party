services:
  burger-party:
    build: .
    container_name: burger-party
    ports:
      # host:container — change the host port if 3000 is taken on your Ubuntu box
      - "3000:3000"
    volumes:
      - burger_party_data:/app/data
    restart: unless-stopped

volumes:
  burger_party_data:
